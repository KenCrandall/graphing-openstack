Single yaml file per service that graphs the servers that make up the service.
Model which server initiates communication with other servers.

Just like the OpenStack wide graph, an arrow from A -> B indicates A
initiates a transition with B. Unlike the OpenStack wide graph, all arrows
are the same type.
